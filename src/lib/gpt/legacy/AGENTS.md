This directory is archival. Do not delete, rename, or bulk-clean files here unless the user explicitly requests that exact action.

Default policy:
- Move deprecated GPT code into this directory when retiring active paths.
- Keep legacy files readable for reference.
- Do not remove this directory or its contents as part of cleanup work.
