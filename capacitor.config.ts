import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.alliance617.emotionaldiary",
  appName: "bible-cbt-diary",
  webDir: "out",
};

export default config;
